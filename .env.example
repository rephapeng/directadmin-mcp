# DirectAdmin connection. Copy to .env and fill in DA_USER.
DA_URL=https://vega.jetorbit.net:2222
DA_USER=
DA_KEY=

# Set to 1 only if the panel uses a self-signed certificate.
# vega.jetorbit.net has a valid cert, so leave this off.
# DA_INSECURE_TLS=1
